import React from 'react';
import { HeaderWrapper, HeaderTitle } from '../PageHeader/PageHeader.styles';
export function PageHeader() {
  return (
    <HeaderWrapper>
      <HeaderTitle>Santi's Shop</HeaderTitle>
    </HeaderWrapper>
  );
}
